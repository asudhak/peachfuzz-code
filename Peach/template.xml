<?xml version="1.0" encoding="utf-8"?>
<Peach xmlns="http://phed.org/2008/Peach" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://phed.org/2008/Peach /peach/peach.xsd">

	<!-- Import defaults for Peach instance -->
	<Include ns="default" src="file:defaults.xml"/>

	<!-- TODO: Create data model -->
	<DataModel name="TheDataModel">
		<Block>
			<String value="Hello "/>
			<String value="World!" />
		</Block>
		<Choice>
			<String value="FOO" />
			<String value="BAR" />
		</Choice>
	</DataModel>

	<Agent name="LocalAgent" location="tcp://127.0.0.1:9001" >
		<Monitor class="debugger.WindowsDebugEngine">
			<Param name="CommandLine" value="C:\Program Files (x86)\Mozilla Firefox\firefox.exe fuzzed.txt"/>
			<Param name="StartOnCall" value="ScoobySnacks"/>
		</Monitor>
	</Agent>

	<!-- TODO: Create state model -->
	<StateModel name="TheState" initialState="Initial">
		<State name="Initial">
			<!--<Action type="output" publisher="Stdout">
				<DataModel ref="TheDataModel"/>
			</Action>-->
			
			<Action type="output" publisher="FileWriter">
				<DataModel ref="TheDataModel"/>
			</Action>
			<Action type="close" publisher="FileWriter"/>

			<Action publisher="Peach.Agent" type="call" method="ScoobySnacks" />
		</State>

	</StateModel>

	<Test name="TheTest">
		<Agent ref="LocalAgent"/>
		<StateModel ref="TheState"/>
		<Publisher class="Console" name="Stdout" />
		<Publisher class="FileStream" name="FileWriter">
			<Param name="FileName" value="fuzzed.txt"/>
		</Publisher>
	</Test>

	<!-- Configure a single run -->
	<Run name="DefaultRun">
		<Test ref="TheTest"/>
	</Run>

</Peach>
<!-- end -->
