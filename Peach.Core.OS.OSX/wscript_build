#!/usr/bin/env python

deps = [
	'Peach.Core.dll',
	'System.dll',
	'System.Core.dll',
	'NLog.dll',
]

bld(
	features = 'cs',
	use = deps,
	source = bld.path.ant_glob('**/*.cs'),
	name='Peach.Core.OS.OSX.dll',
)
